// function setup() {
//     createCanvas(windowWidth, windowHeight); // Create canvas that covers the whole window
//     noLoop(); // Only draw once
// }

// function draw() {
//     // Set a gradient background
//     for (let i = 0; i <= height; i++) {
//         let inter = map(i, 0, height, 0, 1);
//         let c = lerpColor(color('#3c8453'), color('#bcaa96'), inter);
//         stroke(c);
//         line(0, i, width, i);
//     }
// }

// function windowResized() {
//     resizeCanvas(windowWidth, windowHeight); // Resize the canvas when the window is resized
// }

